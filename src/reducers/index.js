
import {filters, filter_data} from './filters'
import {charts} from './charts'
import {navBar, layouts} from './layouts'

export const reducers = {
    filters: filters,
    filter_data: filter_data,
    charts: charts,
    navBar: navBar,
    layouts: layouts
}
